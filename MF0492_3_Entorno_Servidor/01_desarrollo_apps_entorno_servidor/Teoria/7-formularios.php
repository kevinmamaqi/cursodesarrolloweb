<?php

// LAS SUPERGLOBALES DE PHP $_GET y $_POST.
// Las superglobales son variables del propio idioma PHP,
// accesibles desde todas partes.

// $_SERVER superglobal que contiene toda la información de una petición concreta.
// Varia en función de cada servidor. Se genera (como el propio nombre indica) desde el servidor.

// htmlspecialchars()

// $_SERVER["REQUEST_METHOD"]